import React from 'react';
import { NavLink } from 'react-router-dom';

const ProjectList = () => (
  <div>
    ProjectList
    <NavLink to="/createProject"> Добавить проект</NavLink>
  </div>
);
export default ProjectList;
